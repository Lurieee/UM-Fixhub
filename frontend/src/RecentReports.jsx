import { MapPin } from "lucide-react"; import Icon from "./Icon"; import { statusStyles } from "./mockData";
export default function RecentReports({ reports, onNavigate }) { return <section><div className="flex items-center justify-between mb-4"><h2 className="text-xl font-extrabold">Your Recent Reports</h2><button onClick={() => onNavigate("/my-reports")} className="text-sm font-bold text-[#b40012]">View All Reports</button></div><div className="space-y-4">{reports.map((r) => { const s = statusStyles[r.status]; return <a key={r.id} href={"/my-reports/"+r.id} onClick={(e) => { e.preventDefault(); onNavigate("/my-reports/"+r.id); }} className="flex items-center justify-between bg-white rounded-xl px-5 py-4 shadow-sm"><div className="flex items-center gap-4"><span className="grid place-items-center w-11 h-11 rounded-lg bg-orange-100"><Icon name={r.icon} className="w-5 h-5 text-[#b40012]"/></span><div><b>{r.title}</b><p className="text-sm text-slate-500 mt-1 flex gap-1"><MapPin className="w-4 h-4"/>{r.location}</p></div></div><span className={`text-xs font-bold px-3 py-1.5 rounded-full ${s.bg} ${s.text}`}>{s.label}</span></a>; })}</div></section>; }
